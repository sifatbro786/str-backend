# ─── Server ───────────────────────────────────────────────
NODE_ENV=development
PORT=5025

# ─── CORS ─────────────────────────────────────────────────
# Comma-separated list of allowed origins (no trailing slash).
CORS_ORIGINS=http://localhost:3000

# ─── Database (added in a later phase) ────────────────────
# MONGODB_URI=mongodb+srv://strsolutionsltd_db_user:GBomaQL7TEHLzT7m@cluster0.rt9yqps.mongodb.net/str?appName=Cluster0

# ─── Auth (added in a later phase) ────────────────────────
# JWT_SECRET=ksbfjsbdkjfbfbhksjdbf46465sdfsdf
# JWT_EXPIRES_IN=7d
